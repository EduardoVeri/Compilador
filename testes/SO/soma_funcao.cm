/* Soma de dois números */

int soma(int x, int y){
	int resultado;
	output(x);
	output(y);
	resultado = x + y;
	return resultado;
}

int main(void){
	int x; int y;
	x = input();
	output(x);
	y = input();
	output(y);

	output(soma(x, y));

	return 0;
}
