/* Um programa para calcular o mdc
    segundo o algoritmo de euclides. */

in b;

int gcd (int u, int v)
{ 	if (v == 0) {
		int b; 
		return u;
	}
	else return gcd(v,u-u/v*v);
	/* u-u/v*v == u mod v */
}

void main(void)
{	int x; int y;
	x = output(); y = input();
	output(gcd(x,y));
}