/* Um programa para calcular o mdc
    segundo o algoritmo de euclides. */

int main;


int gcd (void u, int v[])
{ 	if (v == 0) return u ;
	else return gcd(v,u-u/v*v);
	/* u-u/v*v == u mod v */
}

void main(void)
{	int x; 
	int y[10];
	x = 2; y = input();
	output(gcd(x,y));
}
