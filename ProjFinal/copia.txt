/* Um programa para calcular o mdc
    segundo o algoritmo de euclides. */

int b;

int gcd (int u, int v)
{ 	if (v == 0) { 
		b[20]; /* Verificar esse erro */
		return b[40];
	}
	else return gcd(v,u-u/v*v);
	/* u-u/v*v == u mod v */
}

void main(void)
{	int x; int y;
	x = output(); y = input();
	output(gcd(x,y));
}