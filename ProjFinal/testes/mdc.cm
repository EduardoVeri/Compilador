/* Um programa para calcular o mdc
    segundo o algoritmo de euclides. */


int vetor[10];

int gcd (int u, int v)
{ 	
	vetor[u] = vetor[10+10+20];
	u = u + 4;
	if (v != 4) {u = 10;}
	else return gcd(v,u-u/v*v);
	/* u-u/v*v == u mod v */
}

void main(int u)
{	int x; int y;
	x = input(); y = input();
	output(gcd(x,y));
}
