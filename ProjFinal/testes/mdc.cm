/* Um programa para calcular o mdc
    segundo o algoritmo de euclides. */

int gcd (int u, int v)
{ 	
	output(u);
	output(v);
	if (v == 0) {return u;}
	else return gcd(v,u-u/v*v);
}

void main(void)
{	int x; int y;

	x = 12; y = 12;
	output(x);
	output(y);
	output(gcd(x,y));
}
